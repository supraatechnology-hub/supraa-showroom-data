name: Precompute Room Masks (SAM 3)

on:
  workflow_dispatch:
    inputs:
      room_id:
        description: "Leave blank to process every room in rooms_manifest.json. Fill in to do just one (e.g. kitchen_01)."
        required: false
      client_slug:
        description: "Client slug — cosmetic only, not used in any file path"
        required: false
        default: "demo"
      category:
        description: "Required only when room_id is set (e.g. kitchen, bathroom, living_room)"
        required: false

jobs:
  segment:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    env:
      HUGGING_FACE_HUB_TOKEN: ${{ secrets.HF_TOKEN }}
      R2_ENDPOINT:            ${{ secrets.R2_ENDPOINT }}
      R2_ACCESS_KEY_ID:       ${{ secrets.R2_ACCESS_KEY_ID }}
      R2_SECRET_ACCESS_KEY:   ${{ secrets.R2_SECRET_ACCESS_KEY }}
      R2_BUCKET_NAME:         ${{ secrets.R2_BUCKET_NAME }}
      BACKEND_URL:            ${{ secrets.BACKEND_URL }}
      INTERNAL_ROOM_SECRET:   ${{ secrets.INTERNAL_ROOM_SECRET }}

    steps:
      - uses: actions/checkout@v4

      - name: Restore SAM 3 model cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/huggingface/hub
          key: sam3-hf-cache-v1
          restore-keys: sam3-hf-cache-

      - name: Install Python dependencies
        run: |
          pip install --upgrade pip
          pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
          pip install transformers accelerate Pillow numpy boto3 requests

      - name: Run SAM 3 segmentation (all rooms in rooms_manifest.json)
        if: ${{ inputs.room_id == '' }}
        run: python scripts/segment_sam3.py --mode precompute_all

      - name: Run SAM 3 segmentation (single room)
        if: ${{ inputs.room_id != '' }}
        env:
          ROOM_ID:     ${{ inputs.room_id }}
          CLIENT_SLUG: ${{ inputs.client_slug }}
          CATEGORY:    ${{ inputs.category }}
        run: |
          python scripts/segment_sam3.py \
            --mode precompute \
            --room-id "$ROOM_ID" \
            --category "$CATEGORY" \
            --client-slug "$CLIENT_SLUG"

      - name: Validate masks (single room)
        if: ${{ inputs.room_id != '' }}
        env:
          ROOM_ID:     ${{ inputs.room_id }}
          CLIENT_SLUG: ${{ inputs.client_slug }}
        run: |
          python scripts/validate_mask.py \
            --client-slug "$CLIENT_SLUG" \
            --room-id "$ROOM_ID"

      - name: Mark room ready via backend (single room)
        if: ${{ inputs.room_id != '' }}
        env:
          ROOM_ID: ${{ inputs.room_id }}
        run: |
          python scripts/update_room_status.py --room-id "$ROOM_ID"
